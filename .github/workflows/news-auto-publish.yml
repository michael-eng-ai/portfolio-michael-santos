name: Auto Sync News

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */2 * * *"

concurrency:
  group: auto-sync-news
  cancel-in-progress: true

jobs:
  sync-news:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check database configuration
        id: guard
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          if [ -n "${DATABASE_URL:-}" ]; then
            echo "db_ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "db_ready=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=Database not configured::DATABASE_URL secret is not set; skipping news sync. Add it under Settings -> Secrets and variables -> Actions to resume."
            {
              echo "## Skipped: database not configured"
              echo ""
              echo "\`DATABASE_URL\` is not set, so the news sync step was skipped instead of failing."
              echo "Add the \`DATABASE_URL\` secret (VM PostgreSQL connection string) to resume automatic news sync."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Sync RSS news to PostgreSQL
        if: steps.guard.outputs.db_ready == 'true' && vars.VM_OWNS_NEWS_SYNC != 'true'
        run: pnpm content:sync:news
        env:
          DATABASE_PROVIDER: postgres
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_SSL: ${{ secrets.DATABASE_SSL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
